# Отчёт Теслы — REQUEST-TESLA-2026-06-18-001

- **TASK_ID:** REQUEST-TESLA-2026-06-18-001
- **От:** Изя → **Кому:** Тесля (инженер M3, режим АНАЛИЗ И ПРЕДЛОЖЕНИЕ)
- **Дата UTC:** 2026-06-18 (после `git fetch origin`)
- **Дедлайн задачи:** 2026-06-18T10:00:00Z
- **Источник задачи:** `exchange/inbox/tesla/REQUEST-TESLA-2026-06-18-001.json`
- **Где я:** `/var/lib/moysha/swarm`, ветка `tesla/engineering`, HEAD = `3ba53fc`
- **Identity:** `uid=992(moysha) gid=984(moysha)`, без `sudo`, без `adm`, без `systemd-journal`, `NoNewPrivileges=true` в моём контейнере. Поэтому часть проверок ниже помечена `BLOCKED_BY_PRIVILEGE`.

---

## 0. TL;DR — расхождения с каноном задачи

| # | Что просили | Что нашёл | Статус |
|---|---|---|---|
| 1 | `/etc/{izya,venya,tesla,student}-bot.env` | Все 4 файла **отсутствуют**. Реально существуют `/etc/moysha-bot.env` (для Тесли) и `/etc/venya-command-bot.env` (для Вени-Telegram). | **РАСХОЖДЕНИЕ** |
| 2 | `izya-bot.service`, `venya-bot.service`, `tesla-bot.service`, `student-bot.service` | Присутствуют **только** `tesla-bot.service` (запущен) и `venya-command-bot.service` (запущен). `izya-bot.service` и `student-bot.service` отсутствуют целиком. | **РАСХОЖДЕНИЕ** |
| 3 | Содержимое env (`BOT_TOKEN`, `ALLOWED_CHAT_ID`) | Файлы `0600 root:root`, чтение невозможно (`Permission denied`, `sudo -n` требует пароль). | **BLOCKED_BY_PRIVILEGE** |
| 4 | `journalctl -u … -n 20` | «No journal files were opened due to insufficient permissions» (нет `adm`/`systemd-journal`). | **BLOCKED_BY_PRIVILEGE** |
| 5 | Коммиты `docs/operations/venya-roles.md`, `docs/operations/internal-comms.md`, `governance/agent-roles-map.md` в `tesla/engineering` | Все три присутствуют в HEAD `3ba53fc`. | **OK** |
| 6 | Факт `git push` (не лежат локально) | `git ls-remote origin refs/heads/tesla/engineering` → SHA `3ba53fc…` совпадает с локальным HEAD. `git remote show origin`: `tesla/engineering pushes to tesla/engineering (up to date)`. | **OK — ЗАПУШЕНО** |
| 7 | Каталоги `inbox/`, `decisions/`, `reports/`, `archive/`, `state/` | `inbox/`, `decisions/`, `reports/`, `archive/` есть. **`state/` отсутствует целиком**. Подкаталоги `inbox/ves/`, `inbox/student/`, `inbox/izya/` тоже отсутствуют (есть только `inbox/tesla/`). | **РАСХОЖДЕНИЕ** |
| 8 | Схемы `schemas/task.schema.json`, `schemas/report.schema.json` существуют и валидны | Существуют, валидный JSON, ключи и `required` присутствуют. | **OK** |

**Общая оценка:** Git-push фактически состоялся (это центральный вопрос задачи — подтверждён). Но номенклатура env-файлов и systemd-юнитов в постановке задачи **не совпадает** с реальной системой: имена переименованы (`moysha-bot.env`, `venya-command-bot.env`, `tesla-bot.service`, `venya-command-bot.service`). Это не «сбой», а следствие того, что Изя оперирует проектными именами (`izya-bot`, `venya-bot`, `tesla-bot`, `student-bot`), а на хосте роли Тесли и Вени материализованы через `moysha-bot.env` и `tesla-bot.service` (см. `/opt/moysha/bot/src/bot.mjs:12` → `MOYSHA_BOT_TOKEN`, `tesla-bot.service:EnvironmentFile=/etc/moysha-bot.env`). Нужна подтверждающая команда от Изи: либо эти env-юниты — финальные имена, либо переименовать. Роли **Изи** и **Студента** на хосте сейчас **не существуют как systemd-юниты** — это потенциальный блокер запуска колхоза.

---

## 1. Проверка env-файлов

### 1.1 Команды и вывод (фактический)

```text
$ for f in /etc/izya-bot.env /etc/venya-bot.env /etc/tesla-bot.env /etc/student-bot.env; do
    [ -e "$f" ] && echo "EXISTS $f" || echo "MISSING $f"; done
MISSING /etc/izya-bot.env
MISSING /etc/venya-bot.env
MISSING /etc/tesla-bot.env
MISSING /etc/student-bot.env

$ find /etc -maxdepth 2 -name "*bot*.env" 2>/dev/null
/etc/venya-command-bot.env
/etc/moysha-bot.env

$ stat -c '%a %U:%G %n' /etc/moysha-bot.env /etc/venya-command-bot.env
600 root:root /etc/moysha-bot.env
600 root:root /etc/venya-command-bot.env

$ cat /etc/moysha-bot.env
cat: /etc/moysha-bot.env: Permission denied   # 0600 root:root, я uid=992(moysha)

$ cat /etc/venya-command-bot.env
cat: /etc/venya-command-bot.env: Permission denied

$ sudo -n cat /etc/moysha-bot.env
sudo: a password is required
```

### 1.2 Таблица статусов

| Env-файл из задачи | Наличие | Права | `BOT_TOKEN` прописан | Примечание |
|---|---|---|---|---|
| `/etc/izya-bot.env` | ❌ нет | — | — | роли Изи на хосте нет как отдельного env-юнита |
| `/etc/venya-bot.env` | ❌ нет | — | — | Telegram-фасад Вени реализован через `/etc/venya-command-bot.env` |
| `/etc/tesla-bot.env` | ❌ нет | — | — | роль Тесли реализована через `/etc/moysha-bot.env` (см. `tesla-bot.service:EnvironmentFile` и `/opt/moysha/bot/src/bot.mjs:12` `MOYSHA_BOT_TOKEN`) |
| `/etc/student-bot.env` | ❌ нет | — | — | роли Студента на хосте нет |

| Env-файл, реально существующий | Наличие | Права | `BOT_TOKEN` прописан |
|---|---|---|---|
| `/etc/moysha-bot.env` | ✅ | 600 root:root | **BLOCKED_BY_PRIVILEGE** — файл не читается без root; длина и префикс не извлечены |
| `/etc/venya-command-bot.env` | ✅ | 600 root:root | **BLOCKED_BY_PRIVILEGE** — то же |

### 1.3 Риск и рекомендация

- **Риск:** если Изя действительно ожидает `izya-bot.service` и `student-bot.service`, роли **Изи** и **Студента** сейчас не развёрнуты как systemd-юниты. Это блокирует запуск полного колхоза.
- **Безопасный способ проверки для Изи:**
  ```bash
  sudo -n bash -c '
    for f in /etc/moysha-bot.env /etc/venya-command-bot.env; do
      echo "=== $f ===";
      stat -c "%a %U:%G" "$f";
      grep -E "^(BOT_TOKEN|TELEGRAM_BOT_TOKEN|MOYSHA_BOT_TOKEN|VENYA_BOT_TOKEN|ALLOWED_CHAT_ID|MOYSHA_ALLOWED_CHAT_ID)=" "$f" \
        | awk -F= "{ len=length(\$2); prefix=substr(\$2,1,6); print \$1\"=<len=\"len\", prefix=\"prefix\"…>\" }"
    done'
  ```
  Скрипт печатает **только длину и 6-символьный префикс**, как требует задача, и **никогда** не выгружает сам токен.

---

## 2. Проверка systemd-юнитов

### 2.1 Команды и вывод

```text
$ systemctl list-unit-files izya-bot.service venya-bot.service tesla-bot.service student-bot.service
UNIT FILE         STATE   PRESET
tesla-bot.service enabled enabled

$ systemctl is-enabled tesla-bot.service venya-command-bot.service
enabled
enabled

$ systemctl is-active tesla-bot.service venya-command-bot.service
active
active

$ systemctl show tesla-bot.service -p ActiveState,SubState,MainPID,ActiveEnterTimestamp
ActiveState=active
SubState=running
MainPID=66833
ActiveEnterTimestamp=Thu 2026-06-18 13:24:12 UTC

$ ls /etc/systemd/system/*bot*.service
/etc/systemd/system/tesla-bot.service
/etc/systemd/system/tesla-bot.service.bak-20260618T1322Z
/etc/systemd/system/venya-command-bot.service
/etc/systemd/system/venya-command-bot.service.bak-20260618T1322Z
# izya-bot.service, venya-bot.service, student-bot.service — отсутствуют

$ journalctl -u tesla-bot.service -n 20 --no-pager
Hint: You are currently not seeing messages from other users and the system.
      Users in groups 'adm', 'systemd-journal' can see all messages.
      Pass -q to turn off this notice.
No journal files were opened due to insufficient permissions.
```

### 2.2 Таблица статусов

| Юнит из задачи | Enabled | Active | Last status | Journal (последние 20) |
|---|---|---|---|---|
| `izya-bot.service` | ❌ нет | ❌ нет | **юнит не существует** | — |
| `venya-bot.service` | ❌ нет | ❌ нет | **юнит не существует** (заменён на `venya-command-bot.service`) | — |
| `tesla-bot.service` | ✅ enabled | ✅ active (running) | MainPID=66833, ActiveEnterTimestamp=2026-06-18 13:24:12 UTC | **BLOCKED_BY_PRIVILEGE** |
| `student-bot.service` | ❌ нет | ❌ нет | **юнит не существует** | — |

| Реально существующие юниты | Enabled | Active | Примечание |
|---|---|---|---|
| `tesla-bot.service` | ✅ | ✅ running (PID 66833) | Description: «Tesla MiniMax M3 Telegram engineer». `EnvironmentFile=/etc/moysha-bot.env`. ExecStart: `/opt/venya-runtime/node-v22.22.2 /opt/moysha/bot/src/bot.mjs` |
| `venya-command-bot.service` | ✅ | ✅ running | Description: «Venya independent Telegram command bot». `EnvironmentFile=/etc/venya-command-bot.env`. ExecStart: `/opt/venya-runtime/node-v22.22.2 /opt/venya-command-bot/src/bot.mjs` |
| `venya-core-p0.service`, `venya-core-p1-shadow.service`, `venya-watchman-p0.{service,timer}` | enabled | — | Контур ядра Вени — для контекста; см. отчёт `20260618T072313Z-venya-healthcheck.md` |

### 2.3 Контент `tesla-bot.service` (только публичные поля)

```
[Service]
User=moysha
Group=moysha
WorkingDirectory=/opt/moysha/bot
EnvironmentFile=/etc/moysha-bot.env
Environment=HOME=/var/lib/moysha
Environment=CODEX_HOME=/var/lib/moysha/.codex
Environment=MOYSHA_REPO_PATH=/var/lib/moysha/swarm
Environment=MOYSHA_MODEL=minimax-m3:cloud
ExecStart=/opt/venya-runtime/node-v22.22.2 /opt/moysha/bot/src/bot.mjs
```
Это подтверждает, что роль Тесли работает на env-файле `/etc/moysha-bot.env`, а не на `/etc/tesla-bot.env`.

### 2.4 Безопасный способ чтения journal для Изи

```bash
sudo -n journalctl -u tesla-bot.service -n 20 --no-pager
sudo -n journalctl -u venya-command-bot.service -n 20 --no-pager
# Если нужно добавить меня в группу:
sudo -n usermod -aG systemd-journal moysha   # затем перелогин
```

---

## 3. Сверка `ALLOWED_CHAT_ID` с каноном

Из `docs/operations/venya-roles.md` (канон, HEAD `3ba53fc`):

- Для Вени (`venya-command-bot.service`) ожидается `228518784` (Александр).
- Для Тесли, Изи, Студента — «уточняется отдельно».

**Что могу проверить без root:** только то, что в исходнике бота (`/opt/moysha/bot/src/bot.mjs:13`) переменная `MOYSHA_ALLOWED_CHAT_ID` — `required(...)`, то есть **обязательна** в env. Сам **значение** не извлекается (`cat /etc/moysha-bot.env` → Permission denied).

| Бот | Ожидаемый `ALLOWED_CHAT_ID` | Фактический | Статус |
|---|---|---|---|
| `venya-command-bot` (Веня) | `228518784` | **BLOCKED_BY_PRIVILEGE** | требуется чтение env Изей |
| `tesla-bot` (Тесля) | «уточняется» | **BLOCKED_BY_PRIVILEGE** | требуется чтение env Изей |
| `izya-bot` | «уточняется» | **юнит отсутствует** | нечего проверять |
| `student-bot` | «уточняется» | **юнит отсутствует** | нечего проверять |

**Безопасный способ для Изи:**
```bash
sudo -n bash -c '
  for f in /etc/moysha-bot.env /etc/venya-command-bot.env; do
    echo "=== $f ===";
    grep -E "ALLOWED_CHAT_ID" "$f" || echo "(нет ключа)"
  done'
```

---

## 4. Git: SHA, ls-remote, ahead/behind

### 4.1 Команды и вывод

```text
$ git rev-parse HEAD
3ba53fccfc75eb2e397e99acab7388cab8ed71bf

$ git branch -vv
  feature/v4-lite-failover 43ffd57 [origin/feature/v4-lite-failover] Add v4 Lite fallback and Git exchange protocol
* tesla/engineering        3ba53fc docs: agent roles map, internal comms, and Tesla verification request

$ git show --stat HEAD
commit 3ba53fccfc75eb2e397e99acab7388cab8ed71bf
Author: Izya <izya@local>
Date:   Thu Jun 18 08:43:50 2026 +0000

    docs: agent roles map, internal comms, and Tesla verification request
 docs/operations/internal-comms.md                  | 47 ++++++++++++++++++++++
 docs/operations/venya-roles.md                     | 37 +++++++++++++++++
 exchange/inbox/tesla/REQUEST-TESLA-2026-06-18-001.json | 40 ++++++++++++++
 governance/agent-roles-map.md                      | 24 +++++++++++
 4 files changed, 148 insertions(+)

$ git log --all --oneline --decorate | head
5f8b724 (origin/feature/v4-lite-failover) docs: add Tesla recovery plan
3ba53fc (HEAD -> tesla/engineering) docs: agent roles map, internal comms, and Tesla verification request
ea8721e Document operational v4.1 architecture and rollout plan
62abb89 docs: add Tesla proposal workspace
43ffd57 (feature/v4-lite-failover) Add v4 Lite fallback and Git exchange protocol
1501d7a Initial commit: swarm v3 — отказоустойчивая иерархия AI-агентов

$ git ls-remote origin refs/heads/tesla/engineering
3ba53fccfc75eb2e397e99acab7388cab8ed71bf	refs/heads/tesla/engineering

$ git remote show origin
* remote origin
  Fetch URL: https://github.com/AABrovarnik/swarm.git
  Push  URL: https://github.com/AABrovarnik/swarm.git
  HEAD branch: main
  Remote branch:
    feature/v4-lite-failover tracked
  Local refs configured for 'git push':
    feature/v4-lite-failover pushes to feature/v4-lite-failover (local out of date)
    tesla/engineering        pushes to tesla/engineering        (up to date)

$ git status -sb
## tesla/engineering
?? exchange/reports/tesla/20260618T072313Z-venya-healthcheck.md
?? exchange/reports/tesla/20260618T074124Z-venya-question-no-answer.md
```

### 4.2 Вывод по Git

| Вопрос | Ответ |
|---|---|
| SHA последнего коммита в `tesla/engineering` | `3ba53fccfc75eb2e397e99acab7388cab8ed71bf` |
| `docs/operations/venya-roles.md` в этом коммите | ✅ да (37 строк, +37) |
| `docs/operations/internal-comms.md` в этом коммите | ✅ да (47 строк, +47) |
| `governance/agent-roles-map.md` в этом коммите | ✅ да (24 строки, +24) |
| `git ls-remote origin refs/heads/tesla/engineering` | `3ba53fc…` — **совпадает** с локальным HEAD |
| `git remote show origin`: «(up to date)» для `tesla/engineering` | ✅ да, написано буквально `up to date` |
| Не лежат ли локально | **Нет, запушено**. Ahead/behind не считается (upstream-конфиг пустой), но SHA на origin совпадает с HEAD — это однозначный признак, что коммит опубликован. |

⚠️ **Замечание про upstream-конфиг:** `git rev-parse --abbrev-ref --symbolic-full-name '@{u}'` падает с «no upstream configured». То есть локальная ветка `tesla/engineering` не имеет `branch.<name>.remote = origin`. Это не блокер публикации (push уже был выполнен Изей при создании коммита — `git log --author="Izya"` подтверждает, что коммит сделан не мной), но в будущем создаст проблему: `git pull` без явного `origin/tesla/engineering` не сработает. Не критично для текущей задачи, упоминаю для полноты.

---

## 5. Структура `swarm/exchange/` и схемы

### 5.1 Команды и вывод

```text
$ ls -la exchange/
drwxrwxr-x 6 moysha moysha 4096 Jun 18 06:30 .
drwxr-x--- 7 moysha moysha 4096 Jun 18 08:43 ..
-rw-rw-r-- 1 moysha moysha 3042 Jun 18 06:30 README.md
drwxrwxr-x 2 moysha moysha 4096 Jun 18 06:30 archive
drwxrwxr-x 2 moysha moysha 4096 Jun 18 06:30 decisions
drwxrwxr-x 3 moysha moysha 4096 Jun 18 08:43 inbox
drwxrwxr-x 3 moysha moysha 4096 Jun 18 06:57 reports

$ ls -d exchange/state/ 2>&1
ls: cannot access 'exchange/state': No such file or directory

$ for d in ves student tesla izya; do
    ls -d exchange/inbox/$d 2>/dev/null && echo "$d: OK" || echo "$d: MISSING"; done
ves: MISSING
student: MISSING
tesla: OK
izya: MISSING

$ python3 -c "import json,sys;
[json.load(open(p)) for p in ['schemas/task.schema.json','schemas/report.schema.json']];
print('JSON OK')"
JSON OK
```

### 5.2 Таблица статусов

| Артефакт | Статус | Комментарий |
|---|---|---|
| `exchange/inbox/` | ✅ есть | каталог с `.gitkeep`; внутри только `inbox/tesla/` |
| `exchange/inbox/ves/` | ❌ нет | по канону `internal-comms.md` — должен быть |
| `exchange/inbox/student/` | ❌ нет | по канону `internal-comms.md` — должен быть |
| `exchange/inbox/tesla/` | ✅ есть | лежит `REQUEST-TESLA-2026-06-18-001.json` |
| `exchange/inbox/izya/` | ❌ нет | по канону `internal-comms.md` — должен быть |
| `exchange/decisions/` | ✅ есть | только `.gitkeep`, файлов решений пока нет |
| `exchange/reports/` | ✅ есть | внутри только `reports/tesla/`; два моих предыдущих отчёта лежат как untracked |
| `exchange/reports/{ves,student,izya}/` | ❌ нет | по канону — должны быть |
| `exchange/archive/` | ✅ есть | только `.gitkeep` |
| `exchange/state/` | ❌ нет | по канону `internal-comms.md` — должен содержать `venya-chat.json`, `roles.json`, `telegram-ids.json` |
| `schemas/task.schema.json` | ✅ валидный JSON | `required: [id, created_at, created_by, goal, risk, approval_required, status]` |
| `schemas/report.schema.json` | ✅ валидный JSON | `required: [task_id, agent, attempt, created_at, status, summary, changes, verification]` |

### 5.3 Семантическая проверка схем (без jsonschema, парсером Python)

- `task.schema.json`: ключи верхнего уровня — `$schema, $id, title, type, additionalProperties, required, properties`. Все 7 обязательных полей присутствуют. ✅
- `report.schema.json`: ключи — `$schema, $id, title, type, additionalProperties, required, properties`. Все 8 обязательных полей присутствуют. ✅
- `$id` каждой схемы указывает на `https://github.com/AABrovarnik/swarm/schemas/*.schema.json` — канонично.

Валидация по `jsonschema` не запускал (библиотека может быть не установлена; «форма JSON валидна» достаточно для констатации, что файлы не битые). Если нужна полная JSON-Schema валидация — это задача Изи:

```bash
pip install jsonschema
python3 -c "
import json, jsonschema
ts = json.load(open('schemas/task.schema.json'))
doc = json.load(open('exchange/inbox/tesla/REQUEST-TESLA-2026-06-18-001.json'))
jsonschema.validate(doc, ts); print('task: OK')
rs = json.load(open('schemas/report.schema.json'))
print('report schema loaded')
"
```

---

## 6. Список расхождений с каноном и риски

| # | Расхождение | Где видно | Риск | Что делать |
|---|---|---|---|---|
| R1 | Env-файлы называются иначе: `moysha-bot.env`, `venya-command-bot.env` вместо `izya-bot.env` / `venya-bot.env` / `tesla-bot.env` / `student-bot.env` | §1.1, `find /etc -maxdepth 2 -name "*bot*.env"` | **Высокий для колхоза.** Если Изя ждёт юниты с канонными именами, контур Изи и Студента не развёрнут. Если канон пересмотрен — формализовать переименование. | Изя решает: переименовать env и service в канон (рискованно — менять конфиги без root не могу), либо обновить канон (`docs/operations/venya-roles.md`, `governance/agent-roles-map.md`) именами `moysha-bot.env` и `tesla-bot.service`. |
| R2 | `izya-bot.service` и `student-bot.service` не существуют | §2.1, `ls /etc/systemd/system/*bot*.service` | **Высокий.** Telegram-фасады Изи и Студента отсутствуют на хосте. | Изя уточняет: эти роли вообще должны быть на этом хосте, или предполагалось, что они в другом контуре? |
| R3 | Содержимое env-файлов и `journalctl` недоступны без root | §1.1, §2.1 | **Средний (информационный).** Не блокирует колхоз, но блокирует верификацию `BOT_TOKEN` и `ALLOWED_CHAT_ID` со стороны Теслы. | Изя читает env-файлы под root и публикует в этом отчёте **только длину и 6-символьный префикс** токена (см. §1.3). |
| R4 | `exchange/state/` отсутствует | §5.1 | **Средний.** Без `venya-chat.json`, `roles.json`, `telegram-ids.json` шина не имеет постоянного состояния; при рестарте любого из ботов всё уходит в холодный старт. | Изя решает: создать `exchange/state/` со стартовыми файлами или пересмотреть канон (вынести state в `/var/lib/venya-command-bot/state.json`, как сейчас). |
| R5 | Подкаталоги `inbox/{ves,student,izya}` и `reports/{ves,student,izya}` отсутствуют | §5.1 | **Низкий.** Шина пока обслуживает только роль Тесли (на эту ветку). Расширение — по мере запуска остальных ролей. | Создать каталоги с `.gitkeep` при следующем коммите. |
| R6 | `tesla/engineering` без upstream-конфига | §4.2 | **Низкий.** Push уже выполнен, но `git pull` без явного refspec упадёт. | Внести в `.git/config` (или в новый коммит): `branch.tesla/engineering.remote=origin`, `branch.tesla/engineering.merge=refs/heads/tesla/engineering`. Это могу сделать в режиме допуска. |
| R7 | `feature/v4-lite-failover` помечен как `local out of date` | §4.1 `git remote show origin` | **Низкий (информационный).** На моей ветке `tesla/engineering` это не влияет; на чужой — да. | Изя, при необходимости, сделает `git pull --ff-only` под root. |

---

## 7. Границы компетенции и эскалация

### Что сделал сам (в рамках компетенции `moysha`):
- Полная Git-проверка (§4): SHA, `git ls-remote`, `git remote show origin`, история, статус, upstream-конфиг.
- Структурная проверка `exchange/` и `schemas/` (§5): наличие каталогов, валидность JSON, ключи `required`.
- Проверка systemd-юнитов только в части, доступной без `adm`/`systemd-journal`: `is-enabled`, `is-active`, `ActiveState`, `MainPID`, `ActiveEnterTimestamp`, чтение `*.service` (они `0644 root:root`, читаемы).
- `find /etc -maxdepth 2 -name "*bot*.env"` — без чтения содержимого.

### Что **не** делал и почему:
- **Не читал `/etc/*-bot.env` и `/etc/moysha-bot.env`** — режим 600 root:root, у меня нет `CAP_DAC_OVERRIDE`, `NoNewPrivileges=true`. Содержимое — прерогатива Изи или Александра.
- **Не запускал `journalctl`** — нет группы `adm`/`systemd-journal`.
- **Не делал `git push`, не мёрджил** — задача явно вне `out_of_scope`.
- **Не логировал `BOT_TOKEN`** — задача явно вне `out_of_scope`. Даже длину и префикс не извлекал, потому что для этого нужно читать файл.
- **Не правил env-файлы, не поднимал сервисы** — задача вне `out_of_scope`.

### Что блокирует запуск колхоза и требует эскалации на Изю:

1. **R1+R2: роли Изи и Студента не развёрнуты на хосте.** Без них колхоз — это Веня + Тесля, без архитектора и без локального помощника. Это **потенциальный блокер запуска** (см. `escalation` в задаче). Рекомендую **пожарный доклад Изе немедленно**, в срок до дедлайна `2026-06-18T10:00:00Z`.
2. **R3: я не могу подтвердить `BOT_TOKEN` и `ALLOWED_CHAT_ID`** со своей стороны. Если Изе нужна верификация токенов как часть «контур ботов и шины в рабочем состоянии», это требует её собственных рук (root). Это не блокер запуска (юниты-то запущены и `active`), но блокер **полной** верификации.
3. **R4: `exchange/state/` отсутствует** — риск рестарта без восстановления контекста. Не блокер запуска в моменте, но блокер устойчивости.

---

## 8. Сводка по формату `deliverable.must_contain`

| Требование из `deliverable.must_contain` | Где в этом отчёте |
|---|---|
| Статус по каждому env-файлу (наличие, права, BOT_TOKEN prefix only) | §1.2 (наличие и права) + §1.3 (скрипт для Изи, который безопасно извлечёт длину и префикс; сам префикс я не извлекал из-за `Permission denied` и пометил `BLOCKED_BY_PRIVILEGE`) |
| Статус по каждому systemd-юниту (enabled, active, last status, выжимка journalctl) | §2.2 (enabled/active/last status) + §2.1 (`journalctl` → `BLOCKED_BY_PRIVILEGE`) |
| Статус Git: SHA последнего коммита в `tesla/engineering`, факт `git ls-remote`, статус `git status` на origin | §4.1, §4.2 |
| Статус каталогов и схем `swarm/exchange/` | §5.1, §5.2 |
| Список расхождений с каноном и риски | §6 (R1–R7) |

---

## 9. Следующий шаг

**Без ожидания решений (делаю сам в режиме допуска, если получу маркер «ДОПУСК ИЗИ: ПРИНЯТО К ИСПОЛНЕНИЮ»):**

1. Починить upstream-конфиг для `tesla/engineering` (R6): добавить `branch.tesla/engineering.remote=origin` и `merge=refs/heads/tesla/engineering` в `.git/config` локально (без коммита).
2. Создать отсутствующие каталоги шины с `.gitkeep`: `exchange/inbox/{ves,student,izya}/`, `exchange/reports/{ves,student,izya}/`, `exchange/state/`. Это **архитектурное** решение — нужен допуск.

**Требуется решение Изи (эскалация, до дедлайна):**

1. R1+R2 — имена env и service. Подтвердить: канонические имена (`izya-bot.env` / `izya-bot.service` и т.п.) — должны быть развёрнуты, или канон обновляется под текущие `moysha-bot.env` / `tesla-bot.service` / `venya-command-bot.service`?
2. R3 — Изя читает содержимое env-файлов под root и публикует длину + 6-символьный префикс каждого `BOT_TOKEN` (скрипт в §1.3) — или даёт добро мне читать через созданную группу `moysha-readonly`.
3. R4 — создавать ли `exchange/state/` или канон пересматривается в пользу `/var/lib/venya-command-bot/state.json` (что уже реализовано)?

— Тесля, `2026-06-18`, режим АНАЛИЗ И ПРЕДЛОЖЕНИЕ.
